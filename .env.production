# 线上环境
ENV = 'production'

# 线上环境接口地址
VITE_API_URL = 'https://sub.store'

# ==================== Docker 部署配置示例 ====================
# 以下配置用于 Docker Compose 部署时的环境变量设置
# 可以通过创建 .env 文件来覆盖这些默认值

# 后端 API 地址 (Docker 网络内通信)
# VITE_API_URL=http://sub-store-backend:3000

# 前端服务暴露端口
# PORT=8888

# Docker 网络配置
# NETWORK_NAME=sub-store-network        # 自定义网络名称
# NETWORK_EXTERNAL=false                # 是否使用外部已存在的网络

# ==================== 使用示例 ====================
# 1. 默认配置启动:
#    docker compose up -d --build
#
# 2. 使用服务名连接后端:
#    VITE_API_URL=http://sub-store-backend:3000 docker compose up -d --build
#
# 3. 加入已存在的外部网络:
#    NETWORK_NAME=my-network NETWORK_EXTERNAL=true docker compose up -d --build
#
# 4. 使用 .env 文件配置:
#    创建 .env 文件并添加配置,然后运行 docker compose up -d --build

